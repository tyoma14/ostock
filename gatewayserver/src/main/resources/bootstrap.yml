spring:
  application:
    name: gateway-server
  profiles:
    active: dev
  cloud:
    config:
      uri: ${CONFIGSERVER_URI}
logging:
  level:
    com.netflix: WARN
    org.springframework.web: WARN
    com.optimagrowth: DEBUG